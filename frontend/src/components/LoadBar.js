import React from "react";

/* Loading Bar when waiting for connection to ETH wallet to connect (MetaMask) */
export default () => {
  return (
    <div className="loader">
      <div className="loaderBar"></div>
    </div>
  );
};
